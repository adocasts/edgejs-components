<div x-data="{
    bordered: false,
    activeAccordion: '',
    setActiveAccordion(id) {
      this.activeAccordion = (this.activeAccordion == id) ? '' : id
    }
  }"
  class="relative w-full mx-auto overflow-hidden text-sm font-normal bg-white rounded-md"
  :class="{
    'border border-gray-200 divide-y divide-gray-200': bordered
  }">

  {{{ await $slots.main() }}}

</div>